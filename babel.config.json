{
  "rules": [
    {
      "test": "/\\.m?js$/",
      "exclude": "/node_modules/",
      "use": {
        "loader": "babel-loader",
        "options": {
          "presets": [
            ["@babel/preset-typescript", "@babel/preset-env", { "targets": "defaults" }]
          ]
        }
      }
    }
  ]
}
